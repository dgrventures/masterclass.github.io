# RUN NN — <short title>

- **Track:** <TAG>  (MA = master/infra/planning · CW = casus-website · JB = lezing · DS = design/style — see dev/project/tracks/)
- **Lock:** no | yes (exclusive repo access — create/remove `dev/LOCK`; see CLAUDE.md §4)
- **Date:** YYYY-MM-DD
- **Commit:** `YYMMDD-<TAG><NN>-<slug>` *(matches folder `<NN>_<TAG>_<slug>`; fill in after committing)*
- **Status:** in progress | done

## Goal
<One or two sentences: what this run sets out to achieve. If it picks up an item
from the handoff §5, name it.>

## Scope
- In scope: <…>
- Out of scope: <…>

## Context read
<Files / sources consulted before starting (brief, handoff, validation workbook,
design skill, …).>

## What changed
<Concrete list of files created/edited under deliverables (or elsewhere). Note
figures that were added or changed and the source they trace to.>

- `path/to/file` — <what & why>

## Decisions
<Choices made and the reasoning — especially anything a future run shouldn't
silently reverse.>

## Website published?
<If the case website changed: yes — copied canonical casepage → index.html +
src/index.html. Otherwise: n/a.>

## Verification
<How you checked the result: numbers cross-checked against the validation
workbook, deck opened, login tested in the deploy environment, etc. State what
was NOT verified.>

## Open / next
<What's left, follow-ups, anything the next run should pick up. Mirror into the
handoff if it's a project-level open item.>
