{
  "name": "ii-projectapp-template",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Impact Institute — per-project web app starter (Azure Static Web Apps + Entra).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:single": "cross-env BUILD_TARGET=single vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "vite": "^5.4.10",
    "vite-plugin-singlefile": "^2.0.3"
  }
}
